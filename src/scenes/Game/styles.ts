import { GameEngine } from "react-native-game-engine";
import styled from "styled-components/native";

export const Container = styled(GameEngine)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`;
